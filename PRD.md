# 메모리룸(MemoryRoom) 개발 5단계 가이드

PRD(제품 요구사항 명세서)를 실제 코드로 옮기기 위한 단계별 진행 순서입니다. 각 단계는 이전 단계가 끝난 뒤에 시작하는 것이 좋고, 한 단계씩 완성해서 눈으로 확인하며 넘어가는 방식을 권장합니다.

> **전제 (가정):** 프론트엔드는 React + Cloudflare Pages, 데이터 저장은 Cloudflare D1(SQL) + R2(이미지)를 기준으로 구성했습니다. 만약 모바일 네이티브(React Native/Flutter)로 방향을 바꾸신다면 1단계부터 다시 조정이 필요해요.

---

## 1단계 — 기초 셋업 & 데이터 구조

**목표:** 앱의 뼈대를 세우고, 모든 정보가 저장될 "데이터 창고"를 먼저 만든다.

**할 일**
- React 프로젝트 생성 (예: `npm create vite@latest memoryroom -- --template react`)
- Cloudflare Pages와 연결 (기존에 다른 앱들 배포하실 때 쓰신 방식과 동일)
- Cloudflare D1 데이터베이스 생성 후, PRD의 데이터 모델대로 표(테이블) 만들기: `Room`, `Pin`, `Anchor`, `ReviewLog`, `User`
- 사진 저장용 Cloudflare R2 버킷 생성
- 폴더 구조 기본 틀 잡기 (예: `src/components`, `src/pages`, `src/api`)

**완료 기준:** 빈 화면이라도 브라우저에서 앱이 뜨고, D1에 표 5개가 만들어져 있으면 성공.

> **용어 설명 — 데이터베이스 "테이블(표)"**: 엑셀 시트라고 생각하시면 됩니다. `Room` 테이블은 "룸(공간)에 대한 정보만 모아둔 시트", `Pin`은 "핀 위치 정보만 모아둔 시트"인 셈입니다.

---

## 2단계 — 룸 생성 & 사진 위 다중 핀 배치

**목표:** 사진을 올리고, 그 사진 위에 여러 개의 번호(핀)를 찍을 수 있게 한다. (PRD 3.1)

**할 일**
- "새 룸 만들기" 화면: 사진 업로드 → R2에 저장 → `Room` 테이블에 기록
- 룸 목록 화면: 그리드 형태로 내가 만든 룸들 보여주기
- **핵심:** 사진 위를 탭하면 그 위치(x, y 좌표)에 핀이 생기는 기능 → `Pin` 테이블에 저장
- 핀 순서를 드래그로 바꾸면 번호가 자동으로 재정렬되는 기능

**완료 기준:** 사진 하나를 올리고, 그 위에 핀을 3~4개 찍은 뒤 새로고침해도 핀 위치가 그대로 남아있으면 성공.

> 이 단계가 이 앱에서 가장 손이 많이 가는 부분이에요. 이미지 위에 정확한 좌표를 찍는 UI (`onClick` 이벤트에서 클릭한 위치의 x, y 값 계산)가 핵심 기술 포인트입니다.

---

## 3단계 — 기억 앵커 등록 (텍스트 + 🎙️음성 입력, 4가지 기법)

**목표:** 각 핀에 실제로 암기할 내용을 넣고, 어떤 기법을 썼는지 기록한다. (PRD 3.2, 4번)

**할 일**
- 핀을 탭하면 뜨는 입력창(모달) 만들기
- 텍스트 입력 필드 + 기법 선택(숫자변환법/문자변환법/연상기억법/기초결합법) 드롭다운
- **🎙️ 음성 입력:** 마이크 버튼 클릭 → `Web Speech API`로 음성을 텍스트로 변환 → 입력창에 자동으로 채워주기
- 저장 시 `Anchor` 테이블에 원본 내용 + 기법 + 입력 방식(텍스트/음성) 기록

**완료 기준:** 마이크 버튼을 누르고 말을 하면 텍스트가 자동으로 입력되고, 저장 후 다시 앱을 켜도 그 내용이 남아있으면 성공.

> **용어 설명 — Web Speech API**: 크롬 같은 브라우저에 이미 내장되어 있는 음성 인식 기능입니다. 별도로 서버를 만들지 않아도 `window.SpeechRecognition` 같은 코드 몇 줄로 바로 쓸 수 있어요.

---

## 4단계 — 복습 모드 & 저장/불러오기

**목표:** 사진만 보고 회상하는 복습 기능을 완성한다. (PRD 3.3)

**할 일**
- 복습 화면: 사진을 크게 보여주고, 핀 위치를 하이라이트
- "정답 보기" 버튼 → 저장된 앵커 내용 표시
- 맞았는지 O/X로 체크 → `ReviewLog` 테이블에 기록
- 순서대로 보기 / 무작위로 보기 옵션
- (선택) 1·3·7·14·30일 간격 반복 알림의 기본 로직 (날짜 계산만 우선 구현, 실제 알림은 5단계에서)

**완료 기준:** 룸 하나를 처음부터 끝까지 복습 모드로 돌려보고, 결과가 통계에 반영되면 성공.

---

## 5단계 — 통계, 마무리 다듬기 & 배포

**목표:** 앱을 실제로 매일 쓸 수 있는 수준으로 완성하고 세상에 내놓는다.

**할 일**
- 통계 화면: 정답률, 취약한 핀(자주 틀리는 곳) 리스트
- 간격 반복 알림 실제 동작(Web Push 또는 앱 켤 때 "오늘 복습할 룸" 안내)
- 다크모드 / 반응형(스마트폰 화면) 다듬기
- 전체 기능 테스트 (특히 음성 입력이 다양한 환경에서 잘 되는지)
- Cloudflare Pages에 최종 배포

**완료 기준:** 폰으로 접속해서 사진 찍고 → 핀 찍고 → 음성으로 암기 내용 넣고 → 복습까지 한 바퀴 돌아가면 완성.

---

## 진행 팁

- 각 단계가 끝날 때마다 "N단계 다음 부분 코드 짜줘"라고 말씀해 주시면, 그 단계에 맞는 실제 코드와 함께 **어느 파일의 어디를 고치면 되는지** 구체적으로 알려드립니다.
- 2단계(다중 핀 배치)가 기술적으로 가장 까다로우니, 여기서 막히면 언제든 편하게 다시 물어봐 주세요.

---

## 원칙 (이번 버전)

- **암기 내용은 항상 사용자가 직접(텍스트 또는 음성으로) 입력합니다.** 외부 API가 자동으로 내용을 채워주는 기능은 이번 버전에는 넣지 않습니다. (3단계의 "텍스트 + 음성 입력"이 이 원칙에 해당)
